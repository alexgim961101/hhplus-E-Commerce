model CouponHistory {
    id        Int      @id @default(autoincrement())
    userId    Int
    couponId  Int
    isUsed    Boolean
    createdAt DateTime @default(now())
    updatedAt DateTime? @updatedAt
}