model OrderProduct {
    id        Int      @id @default(autoincrement())
    orderId   Int
    productId Int
    quantity  Int
    itemTotal Int
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}